export function TopNav() {
  return (
    <header className="topbar">
      <div>
        <p className="eyebrow">Full-stack NLP demo</p>
        <a className="brand" href="/">TF-IDF Summarizer</a>
      </div>
      <nav className="topnav">
        <a href="/">Summarizer</a>
      </nav>
    </header>
  );
}
